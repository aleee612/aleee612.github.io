### 非持续连接
- 每个请求/响应对经一个单独的[[TCP]]连接发送（每次都要验证）
- 即客户端和服务器之间，每进行一次“请求-响应”的对话，就需要先“拨号”建立一次新的[[TCP]]连接，对话结束就“挂断”。下一次对话，需要重新“拨号”。
### 持续连接
- 所有的请求及其响应经相同的[[TCP]]连接发送（就是不会每次都验证）
- 客户端和服务器之间首先建立一条[[TCP]]连接，然后所有的网页资源请求（HTML、CSS、JS、图片等）和它们的响应，都像“货物”一样，通过这条“稳定的传输管道”按顺序来回运送，直到所有工作完成或空闲超时后，管道才会被关闭。