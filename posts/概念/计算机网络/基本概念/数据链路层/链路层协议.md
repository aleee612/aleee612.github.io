- 数据链路层提供链路上**相邻节点间**的逻辑通信
### 提供的服务
- 成帧：所有的链路层协议都要用链路层帧封装
- 链路接入：媒体访问控制协议MAC规定帧在链路上传输，且传输为点对点
- 可靠交付：主要目的为本地纠正而非数据重传；并不是每个协议都提供（如很多有线的就不提供）
- 差错检测 & 纠正：由发送节点在帧中添加差错检验比特，接收方检错
### 网络适配器（网络接口卡NIC）
- 位于主机中，与CPU、存储器通过总线相连
- 发送端：取得数据报并封装成数据帧，然后将帧传进通信链路
- 接收端：取得帧并抽取数据报，如果有检测步骤则执行[[差错检测和纠正]]
