- Java的设计是不针对某种具体平台结构的，像c/cpp不同开发环境的整型范围不同，而Java就不会有这种顾虑，其整型范围是固定不变的
- Java是半编译半解释性的语言
```
JDK
├── JRE（运行环境）
│   ├── JVM（解释器 + JIT）
│   └── JCL（标准类库）
└── 开发工具（javac, javadoc, jar, jdb…）
```
 
- Java的基本文件格式：
	- 有且只有一个`public class` 
	- `public static void main(String[] args)`有点类似c/cpp的main函数