---
aliases:
  - enum
---

```java
public enum Size
{
	Small("S"),Mediom("M");
	private String abb;
	
	private Size(String abb)
	{
		this.abb = abb;
	}
	public String getAbb()
	{
		return abb;
	}
}
```
- 枚举类的构造器总是私有的，private可省略，若声明为public/protect会报语法错
- 所有枚举类型都说Enum的子类，因此会默认继承Enum的方法（具体可查Enum的具体实现）