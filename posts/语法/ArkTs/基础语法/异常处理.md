- `throw`抛出异常，`try`调用

```
function divide(a:number,b:number){
  if(b == 0){
    let error = new Error();
    error.message = "No 0";
    throw error;
  }
  return a / b;
}
try{
  console.log(devide(1,0).tostring());
}
catch(error){
  console.log(error);
}
```