与列表相似，不能修改元素
## 创建与访问：
### 创建：
```python
x = ('a','b',1,2,3)
u = () #创建空元组
```
`*tupe()`：将序列转换为元组*
```python
tuple(list/str/tuple)
```
### 访问：
```python
tuple[index]
```
## 元组的合并与遍历：
### 合并：
```python
z = x + u
```
### 遍历：
```python
for i in x:
        print(i)
```