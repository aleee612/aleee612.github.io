给你一个链表的头节点 `head` 和一个整数 `val` ，请你删除链表中所有满足 `Node.val == val` 的节点，并返回 **新的头节点** 。
![445](https://assets.leetcode.com/uploads/2021/03/06/removelinked-list.jpg)
输入：head = [1,2,6,3,4,5,6], val = 6
输出：[1,2,3,4,5]
```c fold
struct ListNode* removeElements(struct ListNode* head, int val) {
    struct ListNode* p = malloc(sizeof(struct ListNode));
    p->next = head;
    struct ListNode* cur = p;
    for( ;cur->next != NULL ; ){
	    if(val == cur->next->val){
	    cur->next = cur->next->next;
	    }
	    else cur = cur->next;	
    }
    head = p->next;
    free(p);
    return head;
    }
```
- 重点：头结点的判断，否则会造成仅删除头结点后续节点不删除的情况
- 该方法使用虚拟头结点
- 如不适用虚拟头结点，则需另写循环用于判断头结点的情况