### malloc
- malloc 不会初始化 C++ struct 内的成员变量默认值， 只是分配内存
- 不会初始化 data，也不会把 len 设为 0
- 不会调用构造函数
- 不会执行成员默认值
- 内存里面是什么垃圾值就是什么垃圾值
### new
- new 会调用构造函数（包括隐式的、默认的）
- 会执行 struct/class 中的成员默认初始化（如 `int len = 0;`）
- 会保证所有成员处于“已初始化”状态
- 分配失败会抛出异常（而 malloc 返回 NULL）
- 对象的生命周期由 C++ 类型系统管理（可以触发析构）