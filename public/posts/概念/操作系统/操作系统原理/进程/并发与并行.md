### 并发（Concurrency）
- 是指**系统在同一时间段内处理多个任务**，不一定是同时执行，只要任务可以交替进行就算并发。
- 本质：任务之间交替进行，给人的感觉是“同时”，但可能是一个CPU在快速切换上下文。
- 例子：单核 CPU 上运行多个线程，线程通过时间片轮转交替执行。
### 并行（Parallelism）
- 是指**多个任务在同一时间点真正同时执行**。
- 本质：需要多核或多处理器，任务真正同时运行。
- 例子：多核 CPU 上，每个核心执行不同线程，或者 GPU 上的大量线程同时计算。