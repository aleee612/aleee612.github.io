---
aliases:
  - MMU
  - 内存管理单元
---
### 基本概念
- 是cpu里的一种硬件模块
- 实现虚拟地址到物理地址的[[地址绑定#绑定时机：|运行时]]映射，负责把每次内存访问时的虚拟地址翻译成真实的物理地址
### 作用
- 在cpu中，见到的内存地址通常为一个虚拟地址（通常表示为16进制数，例如0x00007ffdf02a0028），而如何将该虚拟地址转换为真实的物理内存地址就是MMU所负责的