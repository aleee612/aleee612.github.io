---
aliases:
  - 方法可见性
---

- `public` > `protected` > `default`(包可见) > `private`
### 具体规则：
- **public**  
    任何地方都能访问（同类、同包、子类、不同包）。
- **protected**
    - 同包下能访问
    - 不同包下的子类也能访问（但要通过[[继承]]关系）
- **default（什么都不写）**
    - 仅限同一个包内能访问
    - 包外的子类都不行
- **private**  
    只有当前类能访问，连子类、同包都不行。